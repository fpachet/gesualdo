# Pre Cleaner-Key Optimization Archive

This archive preserves the generated corpus as it existed before the
2026-06-21 cleaner-key recomputation and notation-review export pass.

The active project tree now keeps only the preferred generated versions:

- cleaner-key MusicXML and MP3 reductions using the `0.05` tessitura tolerance;
- clean conductor-review PDFs generated through `scripts/render_review_pdfs.py`;
- Take 6 output in the preferred `string_quartet_double_stops` variant only.

The active audit after recomputation reports `0 attention` rows in
`outputs/reports/transposition_key_signature_audit.tsv`.
