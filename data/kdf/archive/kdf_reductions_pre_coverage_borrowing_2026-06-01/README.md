# KDF Quartet Reductions Before Coverage Borrowing

This archive snapshots the 37 `data/gesualdo/kdf_reductions/` MusicXML files
and `data/gesualdo/kdf_reductions_report.tsv` before the coverage-borrowing
quartet reducer was applied.

The `Già piansi nel dolore` MusicXML was restored from git before this archive
was made, so this directory is the old baseline for strict comparisons.

Use `SHA256SUMS.txt` for exact file identity checks.
